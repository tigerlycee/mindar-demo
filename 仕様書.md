# MindAR.js ARアクスタ — 仕様書

## 目的

Imagine WebAR 有料版の代替として MindAR.js の精度・使用感を検証する。
精度が許容できれば Unity を使わない純粋な Web 実装に移行する。

## 技術スタック

| 役割 | ツール |
|------|--------|
| ARエンジン | MindAR.js 1.2.5 |
| 3Dレンダリング | A-Frame 1.4.2 |
| ゲームエンジン | なし（Unity 不使用） |
| ホスティング | GitHub Pages（HTTPS 必須） |

## ファイル構成

```
MindAR.js/
├── demo.html         # メインデモ
├── targets.mind      # コンパイル済みマーカー（要生成）
├── butter_512.png    # キャラテクスチャ（akukey からコピー）
├── server.bat        # ローカルサーバー起動
├── 仕様書.md
├── 議事録.md
└── MEMORY.md
```

## セットアップ手順

### 1. targets.mind を生成する

MindAR コンパイラ（ https://hiukim.github.io/mind-ar-js-doc/tools/compile ）で
`butter_512.png` をアップロードし `targets.mind` をダウンロードしてこのフォルダに置く。

### 2. butter_512.png をコピー

```
C:\Users\tigel\Desktop\Antigravity\akkey\akukey\Assets\Image\Chara\butter_512.png
```
をこのフォルダにコピーする。

### 3. ローカルサーバーで確認

`server.bat` を実行 → `http://localhost:8080/demo.html` が PC ブラウザで開く。
スマホで確認する場合は GitHub Pages にデプロイする（camera は HTTPS 必須）。

## 精度判定基準

| 評価項目 | OK | NG |
|----------|----|----|
| 認識速度 | 2秒以内 | 5秒以上 |
| トラッキング安定性 | ほぼズレない | 常に震える |
| 室内照明への耐性 | 普通の蛍光灯で認識 | 認識しない |
| タップ反応 | タップでジャンプする | 反応しない |

## 判定後の方針

- **OK** → MindAR.js 実装に移行（Unity 不要・無料）
- **NG** → Imagine WebAR 有料版を購入（akukey の差し替えコスト約 15 分）
